import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.pexels.com", "res.cloudinary.com",'randomuser.me','lh3.googleusercontent.com'],
  },
};

export default nextConfig;
